CREATE TABLE "Product" (
  "id" varchar PRIMARY KEY,
  "slug" varchar,
  "name" varchar,
  "imageURL" varchar,
  "price" int,
  "description" varchar,
  "createdAt" datetime,
  "updatedAt" datetime
);
